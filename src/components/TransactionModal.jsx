import React from 'react'
import { ArrowUpRight, X } from 'lucide-react'

export function TransactionModal({ transaction, onClose, onSubmit, onDelete }) {
  return <div className="modal-backdrop" onMouseDown={(event) => event.target === event.currentTarget && onClose()}><form className="modal" onSubmit={onSubmit}><div className="modal-heading"><div><p className="eyebrow">{transaction ? 'Edit entry' : 'Quick entry'}</p><h2>{transaction ? 'Edit transaction' : 'Add transaction'}</h2></div><button type="button" className="close-button" onClick={onClose} aria-label="Close"><X size={18} /></button></div><label>Merchant<input name="merchant" defaultValue={transaction?.merchant || ''} placeholder="e.g. Coffee shop" required /></label><div className="form-row"><label>Category<select name="category" defaultValue={transaction?.category || 'Food & dining'}><option>Food & dining</option><option>Transport</option><option>Shopping</option><option>Income</option></select></label><label>Type<select name="type" defaultValue={transaction?.type || 'expense'}><option value="expense">Expense</option><option value="income">Income</option></select></label></div><label>Amount<input name="amount" type="number" min="1" defaultValue={transaction ? Math.abs(transaction.amount) : ''} placeholder="0" required /></label><div className="modal-actions"><button className="secondary-button" type="button" onClick={onDelete} disabled={!transaction}>Delete</button><button className="primary-button" type="submit">{transaction ? 'Save changes' : 'Save transaction'} <ArrowUpRight size={16} /></button></div></form></div>
}
